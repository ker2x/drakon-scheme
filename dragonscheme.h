/* Autogenerated with DRAKON Editor 1.14 */
#ifndef DRAGONSCHEME_H23259
#define DRAGONSCHEME_H23259

#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>


typedef enum {BOOLEAN, FIXNUM} object_type;

typedef struct object {
    object_type type;
    union {
	struct {
            char value;
        } boolean;
        struct {
            long value;
        } fixnum;
    } data;
} object;

object* alloc_object(
    void
);

void eat_whitespace(
    FILE *in
);

object* eval(
    object *exp
);

void init(
    void
);

char is_boolean(
    object *obj
);

char is_delimiter(
    int c
);

char is_false(
    object *obj
);

char is_fixnum(
    object *obj
);

char is_true(
    object *obj
);

void main(
    void
);

object* make_fixnum(
    long value
);

int peek(
    FILE *in
);

object* read(
    FILE *in
);

void write(
    object* obj
);


#endif

